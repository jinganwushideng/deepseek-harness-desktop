<Window x:Class="DeepSeekHarnessDesktop.ImagePreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="预览图" Width="1080" Height="760" MinWidth="640" MinHeight="460"
        WindowStyle="None" ResizeMode="CanResize" Background="Transparent" ShowInTaskbar="False">
  <shell:WindowChrome.WindowChrome><shell:WindowChrome CaptionHeight="48" ResizeBorderThickness="7" GlassFrameThickness="0" CornerRadius="14"/></shell:WindowChrome.WindowChrome>
  <Border Background="{DynamicResource Bg}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="14" ClipToBounds="True">
    <Grid>
      <Grid.RowDefinitions><RowDefinition Height="48"/><RowDefinition Height="*"/><RowDefinition Height="42"/></Grid.RowDefinitions>
      <Border Background="{DynamicResource TitleBarBackground}" BorderBrush="{DynamicResource Border}" BorderThickness="0,0,0,1" shell:WindowChrome.IsHitTestVisibleInChrome="True">
        <Grid Margin="15,0,6,0"><TextBlock x:Name="PreviewTitle" FontWeight="SemiBold" VerticalAlignment="Center"/><Button HorizontalAlignment="Right" Style="{StaticResource IconButton}" ToolTip="关闭" shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="CloseButton_Click"><Path Data="M5,5 L17,17 M17,5 L5,17" Stroke="{Binding Foreground,RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.4" Width="13" Height="13" Stretch="Uniform"/></Button></Grid>
      </Border>
      <Border Grid.Row="1" Background="{DynamicResource Panel2}" Padding="18">
        <ScrollViewer x:Name="ImageScroll" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" PanningMode="Both" PreviewMouseWheel="ImageScroll_PreviewMouseWheel">
          <Image x:Name="PreviewImage" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality">
            <Image.LayoutTransform><ScaleTransform x:Name="ImageScale" ScaleX="1" ScaleY="1"/></Image.LayoutTransform>
          </Image>
        </ScrollViewer>
      </Border>
      <Border Grid.Row="2" Background="{DynamicResource Panel}" BorderBrush="{DynamicResource Border}" BorderThickness="0,1,0,0">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
          <Button Style="{StaticResource SecondaryButton}" Content="－" Padding="12,4" MinHeight="28" Click="ZoomOutButton_Click"/>
          <TextBlock x:Name="ZoomText" Text="100%" Foreground="{DynamicResource Muted}" Width="68" TextAlignment="Center" VerticalAlignment="Center"/>
          <Button Style="{StaticResource SecondaryButton}" Content="＋" Padding="12,4" MinHeight="28" Click="ZoomInButton_Click"/>
          <Button Style="{StaticResource SecondaryButton}" Content="适合窗口" Padding="12,4" MinHeight="28" Margin="8,0,0,0" Click="FitButton_Click"/>
        </StackPanel>
      </Border>
    </Grid>
  </Border>
</Window>
